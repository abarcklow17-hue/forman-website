# Build Project

This workflow compiles the NextJS application.

## Prerequisites
- Ensure you have Node.js 20+ installed.

## Build Steps
1. Install dependencies:
   ```bash
   npm install
   ```
2. Build the production application:
   ```bash
   npm run build
   ```
